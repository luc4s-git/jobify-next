type PaginationProps = {
  currentPage: number;
  totalPages: number;
};

export default function PaginationContainer({
  currentPage,
  totalPages,
}: PaginationProps) {
  return <div>ButtonContainer</div>;
}
